# AI_USAGE.md

## Что делалось с помощью AI

- Изучены `docs/project_requirements.md` и `docs/openapi.auctions.v0.json`; извлечены проектные ограничения и риски контракта.
- SDD-декомпозиция на мелкие задачи; сформулированы AI-правила в `AGENTS.md`.
- Бутстрап React + TS + Vite: package-скрипты, tsconfig, Vite, oxlint, steiger FSD-анализ, oxfmt.
- Tailwind v4 + shadcn/ui (стиль base-mira) на FSD-алиасах (`@shared/ui`, `@shared/lib`); каждый shadcn-примитив разделён на `*.component.tsx` и `*.styles.ts` для `react(only-export-components)`.
- Code-based TanStack Router с типизированным router-контекстом, провайдер TanStack Query, маршруты `/`, `/auctions`, `/auctions/$auctionUuid`, `/auctions/$auctionUuid/bets`, `/auctions/$auctionUuid/bet`. Playwright-smoke (`scripts/route-smoke.mjs`) проверяет редирект, 4 маршрута и 404.
- Hey API codegen (`pnpm codegen`) → `src/shared/api/generated/`, изолирован и исключён из линта.
- Адаптер `shared/api`: `fetchAuctionList`/`fetchAuctionDetail`/`fetchBets`/`placeBet` поверх SDK + нормализация ошибок `ApiError`/`ApiValidationError` для `application/problem+json` и 422.
- Query-ключи `auctionKeys` + `betMutationInvalidationTargets` в `entities/auction`.
- Mock-датасет (`src/shared/api/mocks/`): 10 сид-аукционов, покрывающих все `AuctionStatus`, `AuctionType`, пользовательские ветки `TradingStatus`, restriction-флаги, null/empty-цены, пустые истории ставок, отменённая ставка пользователя; словарь городов, mock-пользователь, 4 конкурента.
- Единое MSW runtime-хранилище (`runtime/store.ts`): чтения + `writeBet` + `resetMockRuntime()`, сид через `structuredClone`. `writeBet` возвращает discriminated union `PlaceBetResult`, чтобы handler'ы мапили failure-ветки в HTTP без повторной валидации.
- MSW-handler'ы на все 4 эндпоинта: `POST /auctions/list` (SDD-011), `GET /auctions/{auctionUuid}` (SDD-012), `GET /auctions/{auctionUuid}/bets` (SDD-013), `POST /auctions/{auctionUuid}/bets` (SDD-014). Все ошибки идут с `content-type: application/problem+json`.
- URL-контракт фильтров списка в `features/auction-filters` (SDD-015): parse/serialize/defaults с round-trip-инвариантом, 47 TDD-тестов. Vitest + Zod установлены, `pnpm test`/`pnpm test:run` добавлены.
- Request builder `buildAuctionListRequest` в `features/auction-filters/lib/request-builder.ts` (SDD-016): транслирует типизированный объект фильтров в тело `AuctionListRequest`, пропуская default-значения, фильтруя UI-only enum (`auc_type: 'Unknown'`), не протекая в admin-only поля. 31 TDD-тест.
- Auctions list query + page composition (SDD-017): TanStack Query hook `useAuctionsList` в `entities/auction/api/` с `select`-маппингом DTO → `AuctionsListViewData` (маппер `toAuctionListItemVM` в `entities/auction/lib/`); prefetch-on-hover через `queryClient.prefetchQuery` во владельце list-page; состояния skeleton/empty/error через ранние return'ы; пагинация через URL search params; типизированный `useSearch` через `validateSearch` на route + кастомные `parseSearch`/`stringifySearch` на router (повторяющиеся ключи для массивов, без default-значений). Page-structure: shell `AuctionsPage` (frame + header) + content `AuctionsList` (hooks + ранние return'ы + success-рендер), Public API слайса экспортирует только shell. Первый реальный потребитель `shared/api` адаптера.
- Auctions list UI redesign (SDD-019): refactor presentation-слоя карточки под modern SaaS-dashboard. Formatters (`formatPrice`/`formatWeight`/`formatVolume`/`formatDate`/`formatPricePerKm`) в `entities/auction/lib/format.ts` с ru-RU `Intl.NumberFormat`/`DateTimeFormat`, NBSP разделитель тысяч, fallback "—" для null/undefined/invalid. Badge-variant mapping'и (`auctionTypeBadgeVariant`/`tradingStatusBadgeVariant`/`auctionStatusBadgeVariant`) в `entities/auction/lib/badge-variants.ts` как `Record<Enum, Variant>`-таблицы с fallback `secondary`. shadcn `Badge` cva расширена тремя semantic variants (`info`/`success`/`warning`) плюс CSS-переменные `--info`/`--success`/`--warning` в `:root` и `.dark` (oklch). Карточка переписана с sectioned-layout: `AuctionCardHeader` (номер + 3 semantic badges) → `AuctionRoute` (largest type, MapPin + ArrowRight иконки) → DatesRow → `AuctionCargo` (compact icon row) → `AuctionTrading` (текущая цена second-largest) → `AuctionAction` (CTA). 8 новых presentation-компонентов в `pages/auctions-list/ui/`, все stateless. VM-mapper расширена полем `tradingStatus: TradingStatus | undefined` (минимальное расширение, чтобы badge-variant решался по enum, а не по label). TDD: 20 тестов для format + 11 для badge-variants (включая fallback cover для каждого enum'а).

## Замечания о текущем покрытии

- UI страницы списка теперь реальный end-to-end: фильтры (SDD-018), карточка (SDD-020) и её SaaS-редизайн (SDD-019). Детальная страница и ставка всё ещё placeholder.
- `SDD-001..014` завершены. `SDD-015` (URL-контракт фильтров), `SDD-016` (request builder), `SDD-017` (list query + composition), `SDD-018` (фильтры UI), `SDD-019` (UI redesign), `SDD-020` (карточка списка), `SDD-021` (detail-страница), `SDD-022` (restriction handling), `SDD-023` (bets nested route), `SDD-024` (bets VM consumer extras — вошло в SDD-023), `SDD-025` (bet form schema), `SDD-026` (bet form route + gate), `SDD-027` (bet mutation integration) завершены. `SDD-028` (runtime consistency verification) + `SDD-030` (final docs) — остались.
- `SDD-005`: TanStack Router в code-based режиме (`RouterProvider` + `QueryClientProvider` в `app.component.tsx`); QueryClient-синглтон в `app/lib/`; маршруты в `app/routes/`, страницы в `pages/<slice>/ui/`.
- `SDD-007`: адаптер превращает `RequestResult` Hey API в `ApiError`/`ApiValidationError`; граница `generated` держится на структуре папок и Public API.
- `SDD-008`: иерархические ключи делают bets потомком detail — план инвалидации читается как документация. steiger-правило `fsd/insignificant-slice` остаётся отключенным: `entities/auction` и `features/auction-filters` имеют по одному потребителю (`pages/auctions-list`) до SDD-018/20.
- `SDD-010`: один module-level `state.auctions` для всех handler'ов; мутация `writeBet` отвергает предыдущую активную ставку, пересчитывает места, обновляет trading-блок в list+detail DTO согласованно. 404 на неизвестном UUID, 422 на `price <= 0`.
- `SDD-011..014`: каждый handler — тонкая HTTP-обёртка над store, владеет только HTTP-конвертом; path-паттерн `*/api/v1/auctions/...` с leading-wildcard работает и в browser-worker, и в Node `setupServer`. Односегментный placeholder `:auctionUuid` + метод-диспатч MSW держат handlers без конфликтов.
- `SDD-014`: спецификация помечает 200-ответ set-bet как `unknown`; адаптер `placeBet` возвращает `void`, mock тем не менее отдаёт `BetItem` — forward-compatible.
- `SDD-015`: контракт в `features/auction-filters` (новый feature-слайс). Реализация без runtime-Zod — это прямая JSON-подобная трансформация; Zod пригодится в SDD-025. URL не содержит admin-only полей (`customer`, `per_page`).
- `SDD-016`: builder `buildAuctionListRequest` — чистая функция `AuctionsListFilters → AuctionListRequest`. Default-значения не отправляются (пустой фильтр → `{}`), `auc_type: 'Unknown'` отсекается (UI-only), `weight_*`/`volume_*` намеренно не маппятся (D-014). Canonical-имя `AuctionListRequest` добавлено в Public API `shared/api` (ранее был только alias `AuctionListFilters`).
- Smoke-скрипты: MSW-list (7 сценариев / 33 assertion), detail (6 / 25), bets (9 / 34), set-bet (9 / ~55) через `npx tsx scripts/msw-*-smoke.mjs`; UI-list (`scripts/list-page-smoke.mjs`, 4 проверки: h1, 10 карточек, hover-prefetch GET, пагинация) через `node scripts/list-page-smoke.mjs` при поднятом `pnpm dev`. В `pnpm check` не входят.

## Какие решения принял кандидат

- **Стек**: Hey API codegen (только SDK+типы), рукописный TanStack Query, code-based TanStack Router, Zustand для точечного UI-state, URL search params как источник истины для фильтров, Tailwind + shadcn/ui, история ставок как вложенный маршрут.
- **Именование**: `*.component.tsx` для всех React-компонентов, `.route.tsx` для маршрутов, `*.test.ts` для тестов (co-locate рядом с тестируемым модулем).
- **Границы**: сгенерированный OpenAPI-слой read-only и изолирован за `shared/api`; query-ключи в одной иерархической фабрике; mock-датасет не реэкспортируется из `shared/api/index.ts`; `msw/browser` изолирован в `mocks/browser.ts` и не реэкспортируется из `mocks/index.ts`.
- **`writeBet` возвращает discriminated union** `PlaceBetResult` вместо бросания — хранилище отвечает за данные, HTTP-семантика за handler'ом (SDD-014).
- **Направление аукциона** (`Down` — младшая выигрывает, `Up` — старшая) — единственный сигнал для ранжирования и перевычисления `available`.
- **Автоматическое отвержение предыдущей активной ставки** пользователя при новой (как в сиде `stoppedRejected`).
- **Path-паттерн `*/api/v1/auctions/...`** с leading-wildcard для browser+Node; односегментный `:auctionUuid` placeholder + MSW-диспатч по методу держат 4 handler'а без конфликтов.
- **MSW worker стартует с `await` перед `createRoot`**, поэтому ни один fetch не уходит до готовности воркера; dynamic import в `import.meta.env.DEV` вырезается Vite в production.
- **`tsx`** (не `--experimental-strip-types`) для smoke-скриптов — extensionless-импорты в generated не требуют renaming.
- **TDD per-task**: тесты пишутся вперёд в задаче, создающей тестируемый код (SDD-015/16/24/25); выделенная задача «Logic Tests» не заводится, TDD встроен в задачи-источники (см. `docs/sdd/decisions.md` D-008).
- **Двусторонний URL-контракт в SDD-015** (D-012): parse + serialize + defaults в одном модуле; default-значения не сериализуются.
- **Display-лейблы для enum'ов** (D-013) в `entities/auction/lib` как `Record<Enum, string>`; первый потребитель — SDD-018.
- **Mock-only расширение `main.auction_uuid`** (D-011) — закрывает контрактный разрыв (paths требуют `auctionUuid`, но DTO его не экспонируют); не протекает в production-типы.
- **Page structure: shell + content.** `AuctionsPage` (frame + header, ноль пропсов, ноль hooks) оборачивает `AuctionsList` (все hooks, ранние return'ы для skeleton/error/empty, success-рендер). Public API слайса экспортирует только shell, content — внутренний.
- **Export-naming без `Component`-суффикса.** Файлы остаются `*.component.tsx` (требование AGENTS.md), но идентификаторы экспорта короткие: `AuctionsList`, `RootLayout`, `Button`. Раньше был mix `AuctionsListComponent`/`Button` — привёл к единому стилю.
- **VM-mapping через `select` в query-hook'е**, не в рендере. `useAuctionsList` имеет `select: toAuctionsListViewData`, который мапит `AuctionListResponse` → `{ items: AuctionListItemVM[], currentPage, lastPage, total }`. Маппинг бегает один раз на новый snapshot, не на каждый render.
- **Типизированный URL search через `validateSearch`.** На route: `validateSearch: parseAuctionsListSearch` (возвращает `Partial<AuctionsListFilters>` — только non-default поля). На router: кастомные `parseSearch`/`stringifySearch` через `URLSearchParams` (повторяющиеся ключи для массивов, undefined/empty пропуски). `useSearch({ from: '/auctions' })` типизирован без `as`-cast'а. Page merge'ит partial с `DEFAULT_AUCTIONS_LIST_FILTERS` для downstream-потребителей.
- **Presentational-компоненты получают generic intent-callback'и.** Карточка принимает `onIntent?: (auctionUuid) => void` (hover/focus-триггер), не `onPrefetch`. Карточка не знает, что page делает с этим сигналом; page решает (prefetch query).
- **SDD-020: VM-mapper централизует nullable-обработку и enum→label.** `toAuctionListItemVM` построен так, чтобы карточка оставалась чистым рендером: каждое nullable-поле DTO схлопывается в `string | null` с явным fallback `''`/`null`, а enum'ы превращаются в русские лейблы через `describe*`. Карточка не импорит `AuctionStatus`/`describeAuctionStatus` и не знает о структуре DTO. 8 TDD-тестов покрывают happy-path, missing-uuid, missing-direction с fallback на города, null-price и missing-trading.
- **SDD-020: логика primary action живёт в чистой функции.** `deriveAuctionCardPrimaryAction` возвращает discriminated union одного из 4 состояний (`place-bet`/`change-bet`/`view-bets`/`disabled`). Терминальные статусы (`Finished`/`Stopped`/`Canceled`) форсят disabled с status-specific лейблом, перебивая `can_set_bet`. 8 TDD-тестов покрывают приоритет и все ветки. Карточка только рендерит результат; SDD-022/23/26 потребляют route без правок логики.
- **SDD-020: stretched-link вместо вложенных `<a>`.** Карточка — это `<article>` с одной stretched `<Link>` поверх всего контента (кликабельно всё) и второй `<Link>` для CTA с `z-10`, чтобы он перебивал stretched-overlay. HTML остаётся валидным (нет вложенных `<a>`), а CTA ведёт на корректный подсегмент (`/bet` или `/bets`) — два разных Link-рендера держат `to` строковым литералом для типизированного `params` TanStack Router.
- **SDD-020: `step` не попал в карточку.** List-DTO `trading.price` содержит только `{start, current, current_no_vat}`; `step` есть только в detail. Карточка не делает вид, что знает шаг, и не дёргает detail-эндпоинт — поле опущено и задокументировано здесь. Если будущая ревизия API добавит `step` в list-DTO, VM-маппер расширяется одним полем без правок UI.
- **SDD-021: VM-mapper `toAuctionDetailVM` повторяет паттерн list'а.** Один `select` в `useAuctionDetail` мапбит `AuctionShowResponse` → `AuctionDetailVM`. Все nullable-поля DTO схлопываются в `string`/`null`/`undefined` с явным fallback; enum'ы (`AuctionType`/`AuctionStatus`/`TradingStatus`/`OperationType`/`PaymentDelayType`/`BidMeasurementType`) → русские лейблы через `describe*`. Страница не импортит типы DTO и не вызывает `describe*` в рендере — только читает VM. 11 TDD-тестов покрывают happy-path, empty-DTO, nullable-цены, restriction-флаги, fallback-лейблы и contact/route-mapping.
- **SDD-021: restriction-флаги изначально читались прямо из VM, без central-handler'а.** SDD-022 тогда ещё не начался — detail тянул `hidePointsAddressAndContacts`/`noViewCargoPrice`/`hideBetsHistory`/`canSetBet` как 4 boolean'а из VM. Замена оказалась ровно такой, как планировалось: одна точка в `AuctionDetailContent` (заменить прямые чтения на `deriveAuctionRestrictions(vm)`), UI-дети не трогаются за исключением сигнатуры. SDD-022 land'нул — Owner модуль живёт в `entities/auction/lib/restrictions.ts`.
- **SDD-021: `CarRequirements.length/width/height` — `number`, не `string`.** OpenAPI-тип говорит `number | null`; mock-seed использует числовые значения (`13.6`, `2.45`, `2.7`). Первая итерация VM ошибочно использовала `string | null` — поймано на typecheck'е. Это контрастирует с `RoutePointCargo.weight/volume`, которые в DTO — строки (строковое представление с 3 знаками после запятой).
- **SDD-021: detail-reuse триггернул вынос 3 badge-компонентов в `entities/auction/ui/`.** SDD-019 явно отложил этот рефактор ("если SDD-021 захочет переиспользовать `AuctionStatusBadge`... тогда вынесем"). Второй потребитель появился — `git mv` 3 файлов в новую директорию + обновление импорта в `auction-card-header`. Никакой логики не добавлено, чистый relocate.
- **SDD-021: 3 новых enum-label helper'а в `describe.ts`.** Добавлены `describeOperationType`/`describePaymentDelayType`/`describeBidMeasurementType` + соответствующие `*_LABELS`-таблицы. Они доменные (русскоязычные), поэтому живут в `entities/auction/lib/`, не в `shared/lib/format/` — повторяет решение SDD-019 про форматтеры цены/веса/объёма.
- **SDD-021: CTA reused через `deriveAuctionCardPrimaryAction` из SDD-020.** Detail-page не дублирует логику primary-action — берёт ту же pure-функцию. Это держит консистентность list-card ↔ detail (одни и те же 4 состояния, один priority disabled-over-canSetBet). "История ставок" добавляется отдельной ghost-кнопкой, гейтится `hideBetsHistory`.
- **SDD-021: error/empty state имеют h1.** First-итерация возвращала только `<Alert>` без заголовка — smoke (`route-smoke.mjs`) упал на `locator('h1')`. Добавлен h1 'Аукцион недоступен'/'Аукцион не найден' в обоих degenerate-состояниях. Это и UX-правильно (screen-reader'у нужен заголовок), и smoke-тесты остаются простыми.
- **SDD-021: ваши ставки vs история ставок — разные секции.** `YourBetCard` рендерит snapshot текущей ставки (`trading.your`); bets-history (SDD-023) будет отдельным nested-route. Карточка не пытается дублировать список — только `last_bet`/`last_bet_with_vat`/`win` из DTO.
- **SDD-022: центральный owner семантики restriction-флагов.** `deriveAuctionRestrictions` в `entities/auction/lib/restrictions.ts` — чистая функция, транслирующая 4 raw-флага (`can_set_bet`/`hide_bets_history`/`hide_points_address_and_contacts`/`no_view_cargo_price`) в positive-named booleans (`canPlaceBet`/`canViewBetsHistory`/`canViewContacts`/`canViewCargoPrice`). 7 TDD-тестов покрывают all-open, каждый флаг индивидуально, fully-locked, независимость. Detail-страница деривирует `restrictions` один раз в `AuctionDetailContent` (через `useMemo`) и пробрасывает в 5 детей (`DetailActionBar`/`ContactsCard`/`RoutesCard`/`TradingCard`/`YourBetCard`) — прямые чтения `vm.hide*`/`vm.canSetBet` в JSX убраны. `deriveAuctionCardPrimaryAction` (SDD-020) получает `canSetBet: restrictions.canPlaceBet` — SDD-020 числится downstream в матрице SDD-022 и читает результат, не сырой флаг. VM `AuctionDetailVM` всё ещё хранит 4 raw-флага (это проекция DTO), но комментарий явно фиксирует: UI читает через `deriveAuctionRestrictions`, не напрямую. Будущие downstream'ы (SDD-023 bets-tab gate, SDD-024 bets VM, SDD-026 bet-form read-only) будут вызывать ту же функцию.
- **SDD-022: контрактное ограничение — list-item DTO не содержит 2 из 4 флагов.** `AuctionListItemTrading` экспонирует только `can_set_bet` и `hide_points_address_and_contacts`; `hide_bets_history` и `no_view_cargo_price` есть только в `AuctionShowTrading`. Карточка списка (SDD-020) физически не может энфорсить `no_view_cargo_price` без detail-данных — матрица приёмки SDD-022 упоминает карточку, но реальный контракт этого не даёт. Это не баг SDD-022, а контрактный предел; если будущая ревизия OpenAPI добавит флаги в list-DTO, `deriveAuctionRestrictions` уже готов принимать любые 4 boolean'а.
- **SDD-023: bets-страница — detail-gated query, не отдельный fetch.** `useAuctionBets(auctionUuid, { enabled: !detail.data?.hideBetsHistory })` — query-hook получает `enabled` из уже-загруженного detail-VM, а не дёргает bets-эндпоинт и потом проверяет флаг. Это означает: restricted-state (`hide_bets_history = true`) физически не делает HTTP-запрос bets — verified через отсутствие сетевого вызова в restricted-сценарии smoke. Побочный эффект: если detail грузится с ошибкой, bets тоже не запрашивается (это правильно — нечего гейтить).
- **SDD-023: VM-mapper `toAuctionBetsVM` повторяет паттерн list/detail.** Один `select` в `useAuctionBets` мапбит `BetListResponse` → `AuctionBetsVM`. Каждая ставка схлопывается в `AuctionBetVM` с nullable-ценами и place, boolean-флагами `isWin`/`isRejected`, и обязательной `cancelReason` (default пустая строка). `participantCount` считает уникальные `organization_id` по всем ставкам (включая отменённые — они тоже участвовали). 9 TDD-тестов покрывают happy-path, empty, nullable, отмену с причиной, счёт участников, сохранение порядка. Страница не импортит DTO и не вызывает `format*` в рендере — только читает VM.
- **SDD-023: detail-first страница — 4 ранних return'а.** `AuctionBets` (content) имеет ветви: detail-pending → skeleton, detail-error → "Аукцион недоступен", detail-vm-missing → "Аукцион не найден", vm.hideBetsHistory → restricted card. Только после этих проверок дёргается `useAuctionBets` со своим skeleton/error/empty/list. Это значит, что unknown-UUID на bets-маршруте показывает "Аукцион недоступен", а не "Bets history" (старое поведение placeholder'а). Smoke обновлён соответственно.
- **SDD-023: Public API `shared/api` расширен типами ставок.** Раньше экспортировался только адаптерный alias `BetsListResponse` (он же `BetListResponse`), без типа элемента. Добавлены `BetItem` и `BetListResponse` — без них entities-слой не мог типизировать mapper, не нарушая FSD-границу (импорт из `generated` напрямую запрещён AGENTS.md).
- **SDD-019: semantic variants через CSS-переменные, не arbitrary Tailwind colors.** Добавлены `--info`/`--success`/`--warning` в `:root` и `.dark` (oklch-цвета), + маппинги в `@theme inline` (`--color-info` etc.). Это следует паттерну существующих `--destructive`/`--secondary` токенов и даёт dark-mode "из коробки". Не использовал arbitrary `bg-blue-500/10 text-blue-700` — это нарушило бы design-system consistency и сломало бы dark mode.
- **SDD-019: 3 новых варианта достаточно (`info`/`success`/`warning`).** `destructive` и `secondary` уже были в Badge cva — переиспользую их для "danger" и "muted" соответственно, не плодя дубликаты. Final variants в badge-variants.ts используют только 6 имён: `secondary`/`info`/`success`/`warning`/`destructive`/`outline`.
- **SDD-019: sub-components в `pages/auctions-list/ui/`, не в `entities/auction/ui/`.** Все 8 новых presentation-компонентов живут рядом с карточкой, потому что единственный потребитель сейчас — list-page. FSD-принцип "не оптимизировать преждевременно": если SDD-021 (detail) позже захочет переиспользовать `AuctionStatusBadge` или `AuctionRoute`, тогда вынесем в `entities/auction/ui/` через осознанный refactor, а не заранее.
- **SDD-019: VM-mapper расширил минимально — добавил `tradingStatus: TradingStatus | undefined`.** Это нужно, чтобы `TradingStatusBadge` мог выбрать badge-вариант по enum, а не угадывать его из русской label-строки. `aucType` и `auctionStatus` уже были в VM; добавление замыкает тройку. Label'ы по-прежнему кэшируются в VM (не пересчитываются в рендере).
- **SDD-019: TradingStatusBadge скрывает NotParticipating/Unknown.** Эти статусы не несут decision-relevant сигнала для перевозчика в list-view; серая pill конкурировала бы за внимание с Leading/Losing/Winner. Скрывается через `HIDDEN`-констату в компоненте, не через mapper — mapper остаётся общей таблицей для всех потребителей.
- **SDD-019: форматтеры в `entities/auction/lib/`, не в `shared/lib/format/`.** Цена/вес/объём с ru-RU локалью и символом `₽` — доменные, не generic. Если позже понадобится `formatFilesize` или `formatDuration`, для них заведём отдельный `shared/lib/format/`. Сейчас спешить некуда.
- **SDD-019: Tooltips НЕ добавлены в v1.** SDD-019 упоминает tooltips для truncated values, но это nice-to-have. shadcn Tooltip + логика detect-overflow — отдельный user-story, не блокирующий релиз редизайна. Отложено до smoke-тестирования с реальными длинными городами/грузами.
- **Route collapse.** `/auctions` схлопнут в один route (parent + child index были boilerplate). `/auctions/$auctionUuid` оставлен parent'ом с `AuctionLayout` — у него 3 child-route'а (index/bets/bet-form) делят path-параметр.
- **Ловушка prop-drilling'а.** Промежуточная попытка вынести `AuctionList` отдельным компонентом с props `query/filters/onIntent/setPage` оказалась проп-дриллингом без реальной изоляции — инлайнила обратно, потом переразделила на shell/content где child самодостаточен (нулевые props у shell'а). Правило: split оправдан, когда у child'а своя ответственность и узкий интерфейс; если надо прокидывать 4+ связанных пропсов — не split.
- **SDD-025: bet-form как feature-слайс.** Новая фича `features/bet-form` повторяет структуру `features/auction-filters` — `lib/` для логики + тестов, `index.ts` Public API. Сама форма (RHF + zodResolver + UI) появится в SDD-027; эта задача владеет только схемой валидации и её TDD-покрытием, без React-рендера. Schema живёт рядом с потребителем, не в `entities/auction/`, потому что валидация ставки — прикладная логика формы, а не доменной модели.
- **SDD-025: Zod-фабрика от constraints, не глобальная константа.** `betFormSchema(constraints)` принимает `{ min?, max?, step?, base? }` — все поля `number | null | undefined`, схлопываются в «нет ограничения». Это позволяет форме читать `trading.price.{min,max,step}` из detail DTO (все nullable по контракту) и передавать их в фабрику as-is; тесты создают синтетические constraints и проверяют behavior без деталей DTO. Глобальная константа не работала бы — ограничения динамические per-auction.
- **SDD-025: один `superRefine` вместо цепочки `.refine()`.** Цепочка refinements собрала бы ВСЕ ошибки (price = -5 под min → две issue'а вместо одной); для UX формы нужно ровно одна ошибка на поле за раз. Реализация: один `superRefine` с early return после первого fail'а. Issue'ы добавляются с `path: []` — на уровне поля `price` это автоматически резолвится в `['price']` на корневом объекте. `z.ZodIssueCode.custom` deprecated в Zod 4.x, используется строковый литерал `'custom'`.
- **SDD-025: step-alignment от `base`, не от 0.** Контракт даёт `step` и `start` отдельно; правило «цена, не кратная шагу от базовой» подразумевает origin. `base` по умолчанию 0, но форма в SDD-027 передаст `base = trading.price.start` (или `current`, в зависимости от семантики), чтобы цена `150` относительно старта `50` при шаге `100` считалась валидной (`150 - 50 = 100` — кратно), а `200` — нет (`200 - 50 = 150`, не кратно). Floating-point epsilon `1e-6` защищает от `0.01 / 0.03`-дрейфа; `step <= 0` отключает проверку (контракт может дать `null`).
- **SDD-025: `available` — UI-подсказка, не part of schema.** Поле `trading.price.available` из detail DTO подсказывает «следующая разумная цена», но контракт не энфорсит его как жёсткое ограничение — поэтому `available` вообще не входит в `BetPriceConstraints`. Форма в SDD-027 может показать его как placeholder/hint; валидация остаётся в границах `min`/`max`/`step`.
- **SDD-025: coercion в `superRefine`, а не через `z.coerce.number()`.** `z.coerce.number()` превращает `''` в `0` (приводит к false-positive «цена обязательна»), а `'abc'` в `NaN` (zod по умолчанию считает NaN валидным number). Чистая реализация — `.any()` + ручная coerc'ия в `toCoercedNumber`: пустая строка/`undefined` → required-ошибка, нечисловая строка → «должна быть числом», `Number.isFinite` отсекает `NaN`/`Infinity`. После всех проверок `.transform()` возвращает финальное `number` для `BetFormValues.price`.
- **SDD-025: нечисловой ввод — структурная ошибка, не исключение.** Zod v4 выбрасывает `Error` на `Number('abc')` только при `z.coerce.number()` без обёртки. Реализация через `Number(value)` + явная проверка `Number.isFinite` держит весь flow в `safeParse()`-семантике: `{ success: false, error: { issues: [{ path: ['price'], message: '...' }] } }`. Это позволяет React Hook Form в SDD-027 читать `error.price?.message` без try/catch.
- **SDD-026: detail-first gate на bet-маршруте.** Страница `/auctions/$auctionUuid/bet` повторяет ранние-return'ы SDD-023 (bets): detail-pending → skeleton, detail-error → "Аукцион недоступен", vm-missing → "Аукцион не найден", `!restrictions.canPlaceBet` → restricted card "Ставка недоступна". Гейт читается через `deriveAuctionRestrictions(vm).canPlaceBet` (SDD-022), не через прямой reading `vm.canSetBet`. Smoke (`scripts/route-smoke.mjs`) покрывает 3 ветки: валидный UUID + can_set_bet=true (форма), `requestWinner` seed can_set_bet=false (restricted), unknown UUID (detail-driven error). Unknown-UUID на bet-маршруте показывает "Аукцион недоступен", а не placeholder "Place or update a bet" — это и UX-правильно, и smoke остаётся простым.
- **SDD-026: shell + content, не placeholder.** Прежний `AuctionBetForm` был placeholder'ом из SDD-005 (с английскими лейблами). Переписан в тот же паттерн shell+content, что и SDD-021/023: `AuctionBetFormPage` (frame-only, zero hooks) — Public API слайса; `AuctionBetForm` (все hooks, ранние return'ы). Route уже был зарегистрирован как child `auctionDetailRoute`; потребовалась только замена `component: AuctionBetForm` → `component: AuctionBetFormPage`.
- **SDD-027: `usePlaceBet` — тонкая обёртка над адаптером.** Мутация в `entities/auction/api/use-place-bet.ts` делегирует `placeBet` адаптеру (HTTP/SDK-конверт — не её ответственность), а сама владеет только кеш-инвалидацией. `onSuccess` инвалидует `betMutationInvalidationTargets(auctionUuid)` — листинг, detail и bets одновременно, чтобы после ставки list/detail/bets показали согласованную картину (цена, trading status, история). Прямая инвалидация трёх ключей документирует намерение и переживёт будущую реструктуризацию иерархии (`betMutationInvalidationTargets` уже явно перечисляет цели).
- **SDD-027: 3-generic `useForm` для input≠output.** Zod-схема принимает `{ price: string | number | undefined }` и возвращает `{ price: number }` (после `.transform()`). RHF v7.83+ принимает `useForm<TFieldValues, TContext, TTransformedValues>` — это позволяет `handleSubmit((values: BetFormValues) => ...)` получать уже типизированный `number`, а `register('price')` работать со строковым полем формы. Без третьего дженерика `zodResolver` сваливался с "Type 'Resolver<{ price: any; }, any, { price: number; }>' is not assignable to type 'Resolver<{ price: string; }, any, { price: string; }>'".
- **SDD-027: `ApiValidationError.validation.errors[]` → RHF field errors.** Бэкенд отдаёт `errors: [{ field: 'price', message: '...' }, ...]` (snake_case path). Форма итерирует массив: `field === 'price'` → `setError('price', { message })` (поверх Zod-ошибки, первая победила через `??=`), прочие поля → `setError('root.serverError', { message: '${field}: ${message}' })`. `isApiValidationError` из `shared/api/errors.ts` уже отличает 422-ответ от прочих `ApiError`; root error рендерится через `<Alert variant="destructive">`, field error — инлайн под input.
- **SDD-027: `available` — placeholder + текстовая подсказка, не hard-constraint.** Schema из SDD-025 не энфорсит `available`; форма показывает его дважды: как `placeholder` в input (быстрый старт для пользователя) и как отдельный `<span>` под полем ("Доступная цена — X ₽") для понимания. Шаг и границы min/max видны в `CardDescription` над формой ("от X · до Y · шаг Z") — это связывает форму с параметрами торгов из detail-VM.
- **SDD-027: success-фидбек через навигацию, не через toast.** SDD-027 spec упоминает toast, но shadcn Sonner (или аналог) в проекте не установлен. Решено: on success форма навигирует на `/auctions/$auctionUuid/bets` — пользователь видит свою ставку в истории сразу после инвалидации (она попадает в top списка, потому что MSW `writeBet` пересчитывает места). Это и есть success-confirmation без новой UI-инфраструктуры. Error-фидбек — inline Alert + field errors. Если позже добавится тост-фреймворк, точки интеграции очевидны (внутри `try/catch` в `onSubmit`).
- **SDD-027: кнопка submit и reset — нативный `<button>`.** shadcn `Button` (base-ui) по умолчанию рендерит `<button>` с `type="button"`; нужно явно передать `type="submit"` и `type="reset"`, иначе форма не отправится. `isPending` состояние отключает обе кнопки и меняет лейбл submit на "Сохранение…", чтобы предотвратить double-submit и дать визуальный отклик.
- **SDD-027: stepper +/- вокруг поля цены.** Когда `step > 0`, поле цены рендерится не как голый `Input`, а как `InputGroup` с двумя `InputGroupButton` (lucide `Minus`/`Plus` иконки) в `inline-start`/`inline-end` addon'ах. Клик `+` вызывает `nextStepPrice(current, constraints)`, `−` — `prevStepPrice`; обе функции чистые и живут в `bet-form-schema.ts` (15 TDD-тестов покрывают missing-step, alignment от base 0 и ненулевого base, clamp к min/max, boundary-кейсы). Snapping — не literal `current ± step`: `Math.floor((current - base) / step)` + 1 (для `+`) или `Math.ceil(...) - 1` (для `−`) гарантирует, что результат всегда step-aligned от `base`, даже если пользователь ввёл misaligned значение руками. Клик `+` из пустого input'а сеет от `available ?? min ?? base ?? 0` и сразу делает шаг вверх — для downLeading seed это даёт `available(44500) → 45000` (ближайшее выравненное от `start`).
- **SDD-027: stepper disable'ится на границах и во время мутации.** `+` disable'ится, когда `nextStepPrice(current) === current` (текущее уже на max или у функции нет шага); `−` — когда `prevStepPrice(current) === current`. Обе кнопки также disable'ятся, пока `placeBet.isPending`, чтобы предотвратить гонку значений во время запроса. Когда `step` в constraints отсутствует или нулевой — stepper вообще не рендерится, поле остаётся plain `Input` (контракт OpenAPI явно даёт `step: null` для части аукционов, и для них +/- кнопки бессмысленны).
- **SDD-027: `scripts/bet-form-smoke.mjs` — отдельный smoke для stepper-интеракции.** route-smoke проверяет только h1/section/alert (URL→content); stepper — это клиентская интеракция с RHF state, которая требует реального click + read input value. Добавлен 6-проверочный smoke: рендер +/- кнопок по aria-label, видимость подсказки "шаг N ₽", seed от available+step на первом клике `+`, корректность повторного шага, обратный шаг, submit-интерактивность. Зарегистрирован в `scripts/smoke.mjs` между route-smoke и msw-browser-smoke.

## Какие AI-предложения были отклонены

- **Не приравнивать `auctionUuid` к `main.order_uid`**: логика роутинга спряталась бы за записью в `decisions.md`; mock-only расширение `main.auction_uuid` (D-011) видимое и forward-compatible.
- Не генерировать React Query-хуки из OpenAPI.
- Не экспортировать сгенерированные артефакты напрямую в feature/entity-слои.
- Не использовать localStorage как источник истины для фильтров.
- Не прятать поток ставок вне иерархии маршрута аукциона.
- Не прыгать в реализацию фич до декомпозиции и архитектурных ограничений.

## Что проверялось особенно внимательно

- OpenAPI-поля с `format: uuid` — Hey API эммитит их как `string`; `order_uid`, `auctionUuid`, UUID сид-аукционов используют каноническую форму.
- Контракто-чувствительные nullable-поля и различия enum'ов между list и detail.
- Валидация и обработка ошибок 422 (`ValidationProblem` с `errors[]`).
- DTO-driven UI-ограничения: скрытые контакты, скрытые цены, скрытая история ставок.
- URL-матчинг MSW: относительный путь сматчился в браузере, но не в Node — leading-wildcard решил обе среды.
- Пагинация за пределами диапазона: `page > last_page` исторически возвращала `meta.from > total`; фикс — `from=0, to=0` для пустой страницы, как Laravel-пагинатор.
- `await worker.start()` до `createRoot` — без race-condition между worker и первым fetch TanStack Query.
- Cross-endpoint консистентность после `writeBet`: smoke проверяет list/detail/bets согласованно.

## Какие риски остаются

- Инфраструктура зрелая, но UI не реализован — страницы placeholder.
- Продуктовые ожидания шире форм ответов OpenAPI, поэтому некоторые UI-значения придётся выводить из доступных данных.
- Схема детальная с многими nullable — риск случайных UI-допущений при реализации.
- Cross-endpoint консистентность проверена smoke как верифицированный инвариант; новый risk — будущие UI-мутации (SDD-027) не должны патчить кеш локально вместо инвалидации query-ключей.
- React Hook Form придёт с UI-формой ставки (SDD-026+); Zod-схема уже в `features/bet-form` (SDD-025).
- shadcn-компоненты шипят с двумя экспортами; каждый `shadcn add` требует ручного разделения на `*.component.tsx` + `*.styles.ts`.
- Playwright-smoke (`scripts/route-smoke.mjs`) требует запущенного dev-сервера, в CI не подключён.
- Адаптер `shared/api` получил первого реального потребителя в SDD-017 (`useAuctionsList` → `fetchAuctionList` → SDK → MSW), но end-to-end-покрытие автоматическими тестами пока только через UI-smoke; logic-тесты по-прежнему покрывают чистую логику, а не adapter-call.
- Query-ключи определены и теперь потребляются: `auctionKeys.list` через `useAuctionsList`, `auctionKeys.detail` через hover-prefetch. `entities/auction` имеет первого потребителя (`pages/auctions-list`).
- Smoke-скрипты закоммичены в `scripts/`, но не в `pnpm check`; browser-smoke требует dev-сервера.
- Mock-only расширение `main.auction_uuid` (D-011) НЕ ДОЛЖНО протекать в production-типы.

## Что улучшилось бы при наличии ещё одного дня

- Заполнить пустые `widgets/` и `features/` слои следующими UI-задачами (SDD-021+: detail, bets, форма ставки, мутации).
- Расширить автоматические тесты помимо чисто логических.
- Уточнить визуальные состояния для мобильных и ошибочных сценариев.
- Расширить mock-сценарии для большего числа edge-case и комбинаций скрытых данных.
- Добавить подробные README-заметки о верификации с матрицей сценариев и скриншотами.
